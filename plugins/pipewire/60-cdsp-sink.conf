# =============================================================
# PipeWire Audio Sink for CDSP Studio / CamillaDSP
# Exposes the cdsp ALSA rate notify plugin as a native system sink
# visible in GNOME / KDE Sound Settings with dynamic rate and channel passthrough.
# =============================================================

context.spa-libs = {
    api.alsa.* = alsa/libspa-alsa
}

context.properties = {
    default.clock.rate          = 48000
    default.clock.allowed-rates = [ 44100 48000 88200 96000 176400 192000 352800 384000 705600 768000 ]
}

context.objects = [
    { factory = adapter
        args = {
            factory.name             = api.alsa.pcm.sink
            node.name                = "cdsp_sink"
            node.description         = "CDSP Studio Dynamic Rate Audio"
            media.class              = "Audio/Sink"
            api.alsa.path            = "cdsp"
            api.alsa.disable-mmap    = true
            audio.allowed-rates      = [ 44100 48000 88200 96000 176400 192000 352800 384000 705600 768000 ]
            adapter.auto-port-config = {
                mode = passthrough
            }
            priority.driver          = 2000
            priority.session         = 2000
        }
    }
]
